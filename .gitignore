.DS_Store
*.swp
# signing / notarization credentials — NEVER commit
.keys
*.keys
# work artifacts / extracted firmware — never commit Apple binaries
*.bin
*.im4p
*.im4m
*.img4
*.dmg
scratch/

# Swift build artifacts
app/.build/
app/VZKextLoader.app/
*.app
DerivedData/

# Bundled runtime (vendored python + r2 + vzkl; regenerated by build-runtime.sh)
app/runtime/
__pycache__/
